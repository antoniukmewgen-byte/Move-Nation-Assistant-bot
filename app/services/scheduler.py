"""Спільний екземпляр APScheduler, доступний з будь-якого модуля.

Раніше планувальник жив як локальна змінна всередині `app/main.py::main()`,
тож жоден інший модуль не міг ним керувати. Тепер нагадування (див.
`app/services/reminders.py`) плануються по одному job'у на групу і ставляться/
знімаються з різних місць — `app/bot/handlers/messages.py` (реакція на
повідомлення в групі) і сам `reminders.py` (первинне планування, повторне
планування, відновлення після рестарту) — тож планувальник має бути
імпортованим модульним синглтоном, а не аргументом, який треба прокидати
через купу функцій.
"""

from apscheduler.schedulers.asyncio import AsyncIOScheduler

scheduler = AsyncIOScheduler(timezone="UTC")
